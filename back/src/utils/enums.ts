export enum StatusUser {
    Active = 'Active',
    Banned = 'Banned',
    Deleted = 'Deleted',
    NotVerified = 'Not Verified',
}

export enum StatusPet {
    Active = 'Active',
    Pause = 'Pause',
    Deleted = 'Deleted',
    Meet = 'Meet',
}
